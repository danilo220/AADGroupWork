<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aadgroup.aadgroupwork">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DotCancellation" />
        <activity android:name=".PathForming" />
        <activity android:name=".RoadSign" />
        <activity android:name=".SquareMatricesCompass" />
        <activity android:name=".SquareMatricesDirections" />
        <activity android:name=".DrawView" />
        <activity
            android:name=".Results"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".menuActivity" />
        <activity android:name=".ClinicianActivity"></activity>
    </application>

</manifest>